import { Text } from '@chakra-ui/react';
import PageWrapper from '../../components/PageWrapper';

export default function MachineStitches() {
  return (
    <PageWrapper title="Machine Stitches">
      <Text mb={2}>
        This page explains different types of machine stitches, their uses, and how to adjust stitch settings on your sewing machine.
      </Text>
    </PageWrapper>
  );
}